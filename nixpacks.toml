[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r python-service/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd python-service && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
