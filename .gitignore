node_modules/

# Environment variables
.env
.env.local
.env.production

.vercel
sync-ads-ai/

# Backups (never commit sensitive data)
BACKUPS/*.sql
BACKUPS/*.sql.gz
BACKUPS/*.sql.zip
BACKUPS/*.zip
backup_*.sql
backup_*.sql.gz
