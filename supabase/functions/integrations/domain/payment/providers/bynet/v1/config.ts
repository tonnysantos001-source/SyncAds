// Endpoints e configurações para o provedor Bynet
export const config = {
  endpoints: {
    production: "https://api-gateway.techbynet.com",
    sandbox: "https://api-gateway.techbynet.com",
  },
  timeoutMs: 8000,
  maxRetries: 3,
};

