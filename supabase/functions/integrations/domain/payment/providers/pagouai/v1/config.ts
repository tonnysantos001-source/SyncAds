// Endpoints e configurações para o provedor Pagou.ai
export const config = {
  endpoints: {
    production: "https://api.pagou.ai",
    sandbox: "https://api-sandbox.pagou.ai",
  },
  timeoutMs: 8000,
  maxRetries: 3,
};
